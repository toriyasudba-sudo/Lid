{
  "name": "toriya-nova-mini-app",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply toriya-nova-mini-app --remote"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
